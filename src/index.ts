import { uploadImage, uploadImages, type UploadOptions, type UploadResult } from './uploadImage';

export { uploadImage, uploadImages };
export type { UploadOptions, UploadResult };

export default {
  uploadImage,
  uploadImages
}; 